// declaro función dividir

function dividir ( a , b ) {
    if ( b == 0 )
       return ("No se puede dividir por cero");

        else return a / b;
}

 // exporto la función
module.exports = dividir

